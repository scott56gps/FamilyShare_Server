{
  "name": "postgres-server",
  "version": "1.0.0",
  "description": "Example Application to query a local postgres database.  The goal is to set up the database before I do anything else with it in heroku.\"",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Scott Nicholes",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "multer": "^1.4.1",
    "pg": "^7.7.1"
  }
}
